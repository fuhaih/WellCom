<Application x:Class="WellCom.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WellCom"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/WellCom;component/Styles/Style.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Image x:Key="PausePng" Source="/WellCom;component/Images/pause.png"/>
            <Image x:Key="PlayPng" Source="/WellCom;component/Images/play.png"/>
            <!--<Style TargetType="ToggleButton" x:Key="PlayButton">
                <Setter Property="Content" Value="{StaticResource PausePng}"/>
                <Style.Triggers>
                    <Trigger Property="IsChecked" Value="True">
                        <Setter Property="Content" Value="{StaticResource PlayPng}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>-->
            <!--<Style TargetType="Menu">
                <Setter Property="Background" Value="{StaticResource WindowColor}"/>
            </Style>-->
        </ResourceDictionary>

        
    </Application.Resources>
</Application>
